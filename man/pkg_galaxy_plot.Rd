% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pkg_galaxy_plot.R
\name{pkg_galaxy_plot}
\alias{pkg_galaxy_plot}
\title{Package Galaxy Plot}
\usage{
pkg_galaxy_plot(sizes = TRUE)
}
\arguments{
\item{sizes}{\code{TRUE} (default) scales point size by each package's
footprint on disk (computed with \code{\link[fs:dir_info]{fs::dir_info()}}). \code{FALSE} draws every
point the same size, which is much faster.}
}
\value{
Invisibly returns the \code{data.frame} used to draw the plot, with
one row per installed package.
}
\description{
Draws a colorful "galaxy" of your installed packages: each package is
placed along a golden-angle spiral, colored by which library it lives in,
and (optionally) sized by its on-disk footprint. Purely decorative, but a
nice way to see how many packages you have and how they're spread across
your libraries.
}
\examples{
\donttest{
pkg_galaxy_plot(sizes = FALSE)
}
}
